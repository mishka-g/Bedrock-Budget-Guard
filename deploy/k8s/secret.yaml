# Replace webhook_url with a real Incoming Webhook, or leave empty and
# set alerts.slack.enabled: false in the config ConfigMap.
apiVersion: v1
kind: Secret
metadata:
  name: budget-guard
  namespace: budget-guard
  labels:
    app.kubernetes.io/name: bedrock-budget-guard
type: Opaque
stringData:
  SLACK_WEBHOOK_URL: ""
